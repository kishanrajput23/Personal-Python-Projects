# Creating Table

A short example that demonstrates building and printing formatted tables in Python (for example using `prettytable` or formatted strings).

**Main script:** `Create_Table.py`

How to run:

1. (Optional) Install PrettyTable: `pip install prettytable`
2. Run: `python Create_Table.py`

This is useful for displaying tabular data in the console.

