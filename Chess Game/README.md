# Chess Game

A small Python implementation that demonstrates chess board setup and basic move logic. This is a learning project to explore game logic and possibly GUI.

**Main script:** `Chess Game.py`

How to run:

1. Run: `python "Chess Game.py"`

The project can be extended with move validation, a GUI, or AI opponent.

